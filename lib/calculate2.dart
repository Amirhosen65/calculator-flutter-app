import 'package:flutter/material.dart';

class calculatepage2 extends StatefulWidget {
  const calculatepage2({Key? key}) : super(key: key);

  @override
  State<calculatepage2> createState() => _calculatepage2State();
}

class _calculatepage2State extends State<calculatepage2> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("AJ"),),
    );
  }
}
